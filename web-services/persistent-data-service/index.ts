import * as objects from './objects'
import * as actions from './actions'

/*
* Enumerate all actions and objects in index
*/
export const persistentDataService =
{
  actions: actions,
  paths: objects
}