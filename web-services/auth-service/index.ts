import * as authServiceObjects from './objects'
import * as authServiceActions from './actions'

/*
* Enumerate all actions and objects in index
*/
export const authService =
{
  actions: authServiceActions,
  objects: authServiceObjects
}
